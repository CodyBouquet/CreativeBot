================================================================
EMAIL REPORTS & NOTIFICATIONS — PLANNING DOC
Created: 2026-04-09
Updated: 2026-04-15
Status: Phase 1 COMPLETE. BMS auth flow clarified. Blocked on Azure.
================================================================

GOAL
----
Add a second tool to the Pi alongside the existing Pipedrive/Arrivy
sync. This one pulls data from the Rollmaster API and emails scheduled
reports to Entra (Azure AD) group members via Microsoft Graph.

On sign-in the user should see a landing page with two action cards:
  - "Pipedrive / Arrivy Sync"  (the existing tool, moves to /sync)
  - "Email Reports"            (the new tool, lives at /reports)


REPORTS WANTED (initial list, more to come)
-------------------------------------------
- Low inventory notification
- Daily sales report    (overall, by salesperson, by branch)
- Weekly sales report   (overall, by salesperson, by branch)
- Monthly sales report  (overall, by salesperson, by branch)

Each report needs:
  - A configurable frequency (daily/weekly/monthly)
  - A configurable send time
  - A target Entra group (recipients resolved at send time)


ARCHITECTURE DECISIONS
----------------------
1. ONE Flask app, TWO tools as blueprints.
   - New file: email_reports.py, registered as a Flask blueprint
   - Shares the existing PIN login
   - "Completely separate" in code, DB tables, and UI — but not a
     separate service. One deploy, one login.

2. Landing page at /
   - Two action cards
   - Current dashboard moves from / to /sync

3. Separate SQLite DB for the new tool: data/reports.db
   - Tables: report_subscriptions, send_history, settings
   - Keeps the two tools isolated — can't corrupt each other

4. Email via Microsoft Graph API (NOT SMTP)
   - Graph is the modern M365 path
   - Same Azure app registration handles mail send + group lookup
   - SMTP would be simpler but can't list group members

5. Recipients = Entra groups, NOT stored email addresses
   - User picks a group in the UI per report
   - At send time: Graph resolves "who's in this group?" and emails each
   - Adding/removing people happens in Entra, Pi picks up automatically
   - No recipient management code in the Pi at all

6. Sender = a shared mailbox (recommended: noreply@creativecarpetinc.com)
   - M365 shared mailboxes are free (no license needed)
   - Create once in Exchange admin center
   - Graph Mail.Send can send as it


WHAT YOU HAVE TO DO IN AZURE (I CAN'T)
--------------------------------------
1. Register an application in Entra ID → App registrations
   - Get me: tenant_id, client_id, client_secret
   - Secret goes into an env var, never committed to git

2. Grant API permissions (APPLICATION type, not delegated)
   - Must be admin-consented:
     - Mail.Send              (to send emails)
     - GroupMember.Read.All   (to resolve group members)
     - Group.Read.All         (to list available groups in UI dropdown)

3. Create the shared mailbox noreply@creativecarpetinc.com
   - Done in Exchange admin center
   - No license needed for shared mailboxes

4. (RECOMMENDED) Scope Mail.Send to only the noreply mailbox
   - Via an Application Access Policy (PowerShell)
   - Prevents the app from accidentally sending as any other user
   - I can provide the PowerShell snippet when ready


BMS (ROLLMASTER) API — WHAT WE KNOW
------------------------------------
Docs:      https://app.theneo.io/broadlume/bms/version
Base URL:  https://api.rmaster.com/api
Auth:      API key via x-api-key header (AWS API Gateway) +
           tenant-scoped token from username/password
Key:       Stored in .env as BMS_API_KEY (never committed)
Creds:     BMS_USERNAME / BMS_PASSWORD in .env (Rollmaster-issued)

Auth flow (confirmed by Broadlume 2026-04-15):
  POST https://api.rmaster.com/api/creativecarpets/token
       body: { username, password }
       headers: x-api-key
       → returns session token
  Use returned token on subsequent calls. Sessions expire after
  ~5 min idle; keepalive endpoint available.

Key endpoint for inventory:
  GET /get-catalog-items-below-safety-stock-limit

Other useful endpoints:
  - Sales Quicklist (by month/date range)
  - Sales Volume by Date
  - Sales Reps
  - Orders by date range
  - Pre-built sales report

RESOLVED (2026-04-15): The 403 was because we were hitting the
generic auth endpoint. Correct path is the tenant-scoped token
endpoint (/creativecarpets/token) with username+password in body.
Credentials received from Broadlume and stored in .env.

Still need to confirm with Broadlume:
  - What does the inventory safety stock response look like?
    Specifically need: items flagged "stocking inventory" with
    on_hand, available, and safety_stock fields.


WHAT I STILL NEED FROM YOU (beyond Azure)
-----------------------------------------
1. Low inventory rules
   - BMS has a "below safety stock" endpoint — is that sufficient,
     or do you want custom thresholds on top of what BMS flags?
   - Scheduled (e.g. check every morning) or event-driven
     (poll every hour, only email when something crosses the line)?

2. Branch list
   - Is "branch" a field on sales records from BMS?
   - Or do I need a hardcoded list of branch names/IDs?

3. Report output format
   - HTML tables in the email body? (my default)
   - PDF attachment?
   - CSV attachment? (good for sales data)
   - Plain text?
   - My recommendation: HTML tables in the body + CSV attached for
     sales reports


PHASED BUILD PLAN
-----------------
PHASE 1 — Foundation ✓ COMPLETE (2026-04-14)
  ✓ Landing page at / with two tool cards (Sync + Reports)
  ✓ Moved sync dashboard from / to /sync
  ✓ Scaffolded Reports blueprint at /reports (email_reports.py)
  ✓ Empty "no reports configured yet" page
  ✓ Created data/reports.db schema (report_subscriptions,
    send_history, report_settings)
  ✓ Updated all internal nav links (logs, settings, users)
  ✓ BMS API key stored in .env

PHASE 2 — Microsoft Graph integration (BLOCKED on Azure work above)
  - Graph client: send test email, list Entra groups, resolve members
  - Settings page to configure tenant_id/client_id/secret (env vars)
  - "Test Send" button in the UI to prove end-to-end before building
    any real reports

PHASE 3 — BMS client + first report (UNBLOCKED 2026-04-15, ready to build)
  - BMS API client (auth, session management, keepalive)
  - First report: low inventory (stocking items below safety stock)
    → fields needed: on_hand, available, safety_stock
  - Scheduler: APScheduler in-process
  - UI to create/edit report subscriptions (pick report type,
    frequency, time, target Entra group)

PHASE 4 — Remaining reports (AFTER phase 3 is proven working)
  - Daily sales report (overall, by salesperson, by branch)
  - Weekly and monthly rollups
  - Sales report uses: Sales Quicklist, Sales Volume by Date,
    Sales Reps endpoints from BMS


OPEN QUESTIONS
--------------
- Is noreply@creativecarpetinc.com the right sender address, or do you
  want a dedicated account?
- Low inventory: is BMS's "below safety stock" flag enough, or do you
  want custom thresholds on top?
- Frequency: check inventory every morning, or poll more often and
  only alert when something newly crosses the threshold?


NEXT STEPS
----------
1. BMS auth unblocked (2026-04-15) — can start now:
   → Smoke test: POST /creativecarpets/token with creds, confirm
     token comes back
   → Hit /get-catalog-items-below-safety-stock-limit, capture the
     response shape (so we know the field names for on_hand /
     available / safety_stock)
   → Build BMS client (auth + session keepalive + typed wrappers)
2. Azure app registered (still waiting on you)
   → Build Graph email client, test send
3. Both done → wire up first report end-to-end
